namespace PrettyEyes.App.Services;

/// <summary>
/// The handful of virtual-key codes the UI has to name. Kept here rather than
/// in the platform project, which the UI does not read constants from.
/// </summary>
internal static class NativeKeys
{
    internal const uint PrintScreen = 0x2C;
}
